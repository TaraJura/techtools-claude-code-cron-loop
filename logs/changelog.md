# Changelog

> Recent changes to the PDF Editor project. Entries older than 7 days are archived to `archive/`.

## Logging Rules

- Log NEW features, bug fixes, security incidents, infrastructure changes
- Do NOT log routine checks or "all passed" messages
- Keep entries concise

---

## 2026-04-10

- **NEW FEATURE** — TASK-129: Print preflight checker. Validates PDFs against print production standards with 10 automated checks: (1) image resolution — effective DPI calculation via operator list transform matrices, flags below 300/150 DPI, (2) font embedding — verifies all fonts are embedded, flags non-embedded as errors, (3) color space analysis — identifies DeviceRGB/CMYK/Gray usage per page, warns on RGB in CMYK-required profiles, (4) bleed & trim boxes — checks TrimBox/BleedBox presence and 3mm bleed compliance, (5) page size consistency — detects mixed page dimensions, (6) transparency — detects blend modes, soft masks, alpha via graphics state operators, (7) thin lines/hairlines — flags strokes below 0.25pt, (8) overprint settings — reports overprint flags, (9) total ink coverage — canvas-based CMYK estimation at sample resolution, flags areas above 300%, (10) PDF version & features — parses header version, checks compatibility. 4 profiles: Standard Print (300 DPI), Digital Print (200 DPI, relaxed), Press Quality (strict, CMYK only), Custom (user-toggled checks). Expandable report sections with pass/warn/fail/info icons and overall score. Export as JSON or printable HTML. Action registry: `run-preflight`, `print-preflight-check`. Keyboard: Ctrl+Shift+P. New file: `js/preflight.js`. Modified: `index.html` (nav tab + tool panel + script tag), `css/tools.css` (preflight styles with dark theme support).
- **NEW FEATURE** — TASK-128: Presenter view with speaker notes, next-slide preview, and session timer. Dual-display presenter experience using BroadcastChannel (`pdf-presenter-sync`): main window shows fullscreen audience display, popup window provides presenter dashboard. Features: (1) three-panel presenter layout (current slide 60%, next slide preview, speaker notes), (2) per-page speaker notes with Markdown formatting (bold/italic/lists), localStorage persistence, font size slider, (3) next-slide preview with end-of-document indicator, (4) triple timer display (elapsed, user-settable countdown with 2-min warning, wall clock), (5) optional audio chime at configurable intervals via Web Audio API, (6) slide navigation with prev/next, page input, thumbnail filmstrip, (7) annotation tools — laser pointer, temporary pen draw (5s auto-fade), spotlight with SVG mask, (8) auto-import of PDF sticky note annotations as speaker notes + JSON import, (9) B/W blank screen with presenter indicator, (10) session summary on end with per-page time tracking and notes export. Action registry: `start-presenter-view`, `stop-presenter-view`. Keyboard: arrows/PageUp/PageDown for nav, N=notes, T=timer, L=laser, P=pen, S=spotlight, B=black, W=white, Esc=end. New file: `js/presenter-view.js`. Modified: `index.html` (toolbar button + script tag).
- **NEW FEATURE** — TASK-127: Scan enhancement toolkit. Auto-detect and fix common scanning artifacts for cleaner documents and better OCR. Features: (1) black border removal with edge histogram analysis and configurable padding, (2) hole punch removal via connected-component analysis with circularity filtering and background-color inpainting, (3) binding shadow removal using column luminance profiling and gradient compensation, (4) background whitening/normalization using histogram peak detection with adjustable intensity, (5) noise reduction (median 3×3/5×5, Gaussian blur), (6) binarization (Otsu automatic, manual threshold, Sauvola adaptive with integral image), (7) batch apply to current/all/even/odd/range pages with progress bar and cancel, (8) preview canvas with original/enhanced toggle, (9) comparison slider integration, (10) 4 presets (Quick Cleanup, Deep Clean, OCR Prep, Black & White). All processing is pure client-side canvas pixel manipulation. Keyboard: Ctrl+Shift+E. 2 action-registry commands (scan-enhance, scan-cleanup). New file: `js/scan-enhance.js`. Modified: `index.html` (nav tab + tool panel + script tag).
- **NEW FEATURE** — TASK-126: Multi-window PDF viewer. Pop out the PDF display into a separate browser window for dual-monitor workflows with real-time BroadcastChannel sync. Features: pop-out viewer with self-contained dark-themed HTML page, bidirectional page/zoom sync via BroadcastChannel (`pdf-editor-sync`), per-axis sync toggles (page/zoom/scroll), independent zoom when sync disabled, "Present on Pop-out" for fullscreen on second screen, heartbeat-based disconnect detection with reconnect overlay, pop-out controls (prev/next/go-to page, zoom, fit-width/page, keyboard nav), multiple pop-out support, blocked pop-up fallback with draggable/resizable detached div. Keyboard: Ctrl+Shift+W. 3 action-registry commands. New file: `js/multi-window.js`. Modified: `index.html` (nav tab + tool panel + script tag), `js/app.js` (viewer-visible tools list).
- **NEW FEATURE** — TASK-125: N-up page layout tool. Arranges multiple PDF pages per sheet for economical printing. Uses pdf-lib embedPages/drawPage for vector-quality output. Features: 6 layout presets (2/4/6/9/16-up + custom rows×cols), 5 paper sizes, portrait/landscape, 3 page ordering modes, configurable margins/gap, 4 scaling modes (Fit/Fill/Shrink/Actual), page range (All/Odd/Even/Custom), optional page borders and labels, repeat page mode for labels/cards, live summary, canvas preview modal, PDF download and print with progress bar. Keyboard: Ctrl+Shift+N. 2 action-registry commands. New file: `js/nup.js`. Modified: `index.html` (nav tab + tool panel + script tag).
- **NEW FEATURE** — TASK-124: Contact sheet generator. Creates a visual thumbnail grid summary of all PDF pages, exportable as PDF or PNG. Features: configurable columns (1-10), paper size (A4/Letter/A3), landscape/portrait orientation, adjustable gap (0-20mm), custom page ranges, optional page number labels and thumbnail borders, optional header text, live layout summary, preview modal, multi-sheet PDF output for large documents, 2x resolution rendering. New file: `js/contact-sheet.js`. Modified: `index.html` (nav tab + tool panel + script tag). 2 action-registry commands (create-contact-sheet, page-overview-grid).

## 2026-04-09

- **NEW FEATURE** — TASK-122: Document snapshot and version manager. Save, browse, compare, and restore named PDF checkpoints using IndexedDB storage. Features: save named snapshots with optional notes; version history timeline sorted by newest with file size deltas; thumbnail preview on hover (pdf.js low-res render); restore any version (reloads into viewer); download individual snapshots as PDF; two-version comparison selection (emits `compare:versions` on bus); auto-snapshot toggle before destructive operations (merge, split, redact, flatten, compress, crop, deskew, protect); export/import full version bundles as ZIP (JSZip); storage management with per-doc and global size display, 100MB warning. Keyboard: Ctrl+Shift+V (quick save), Ctrl+Alt+V (open panel). 3 action-registry commands (save-version, show-versions, restore-version). New file: `js/version-manager.js`. Modified: `index.html` (tab button + tool panel + script tag), `css/tools.css` (version manager styles + dark mode).
- **NEW FEATURE** — TASK-121: Form auto-fill with saved user profiles. Create, manage, and reuse named identity profiles (Personal, Contact, Professional, Custom categories) to auto-populate matching PDF form fields with one click. Features: profile manager modal with full CRUD (create/edit/delete profiles, add custom key-value pairs); smart field matching (exact, alias table, normalized, and partial/substring matching); per-document field mapping memory (localStorage, keyed by filename:size); match preview table with editable values and re-mappable field selectors; "Learn" button to capture manual fills into a profile; import/export profiles with optional AES-256-GCM encryption via Web Crypto API (PBKDF2 key derivation); keyboard shortcut Ctrl+Shift+F. Integrated with: forms.js (`getFormFields`/`setFormFieldValues`), action registry (`forms.autofill`), event bus. New file: `js/form-autofill.js`. Modified: `index.html` (Auto-Fill sub-panel + script tag), `css/tools.css` (auto-fill styles + modal + dark theme + responsive).
- **NEW FEATURE** — TASK-120: Redline document comparison. Text-level track-changes style comparison between two PDF versions — distinct from the existing visual compare tool (pixel-level). Extracts text page-by-page via pdf.js `getTextContent()`, runs Myers diff algorithm at the word level (with fallback to simpler LCS for large documents >10k words), and renders a redline view showing deletions in red strikethrough and additions in blue underline. Features: dual PDF upload zones (original + revised); "Run Redline Comparison" button with progress bar; filter by change type (All/Additions Only/Deletions Only); page navigation with per-page change counts; summary panel (total additions, deletions, pages modified); page-level alignment detection (added/removed pages); export as HTML report (styled, standalone) or JSON; reset to start over. Keyboard shortcut: Ctrl+Shift+R. Registered in action registry (`tools.redline`). New file: `js/redline.js`. Modified: `index.html` (nav tab + tool panel + script tag), `css/tools.css` (redline styles with dark mode support), `js/action-registry.js`.
- **NEW FEATURE** — TASK-119: Gesture-drawn shape recognition (Shape Assist). Optional mode for the freehand drawing tool that auto-recognizes hand-drawn strokes and converts them to clean geometric shapes. Recognizes: straight lines, arrows, rectangles, ellipses/circles, triangles, and diamonds. Uses Ramer-Douglas-Peucker polyline simplification, corner detection via angle analysis, and geometric classifiers (closure ratio, radial variance, angle scoring) with a 0.7 confidence threshold — unrecognized strokes remain freehand. Toggle via "Shapes" button in drawing toolbar or Shift+S shortcut. Persists on/off state in localStorage. Integrated with: action registry (`draw.shapeAssist`), event bus (`shapeAssist:toggled`), undo/redo (recognized shapes use existing drawing types). New file: `js/shape-recognition.js`. Modified: `js/drawing.js` (import + recognition call in handlePointerUp), `index.html` (toolbar button + script tag).
- **NEW FEATURE** — TASK-118: Voice memo annotations. Record short audio comments and attach them to specific positions on PDF pages. Uses MediaRecorder API with Web Audio AnalyserNode for real-time level visualization. Features: click-to-place recording on any page location; inline audio player with play/pause, seek, and duration; sidebar panel listing all memos with sort (page/newest/oldest); drag-to-reposition markers; right-click context menu for rename/delete; export/import as JSON with base64-encoded audio; localStorage persistence keyed by document filename; storage size warning at 4MB; 60-second max recording duration; preferred audio/webm;codecs=opus with fallback. Integrated with: undo/redo (GenericCommand for create/move/rename/delete/clear), action registry, event bus, keyboard shortcut (V). New file: `js/voice-annotations.js`. Modified: `index.html` (tab button + tool panel + script tag), `css/tools.css`, `js/app.js` (added voice-memo to viewer-visible tool list).
- **[SECURITY]** — Fixed innerHTML XSS in `cross-search.js` (TASK-116): group header rendered user filenames (`group.name`) via innerHTML template without escaping — crafted filenames could inject HTML. Replaced with DOM APIs (`createElement`, `textContent`, `title`). Also hardened `errorMsg` rendering to use `textContent` for defense-in-depth.
- **NEW FEATURE** — TASK-116: Cross-document search. Search text across all open PDF tabs simultaneously from a unified floating panel. Features: grouped results by document with collapsible sections and match count badges; text snippets with highlighted matches (±40 chars context); jump-to-result switches tab and navigates to page; search options (case-sensitive, whole-word, regex); result navigation with Enter/Shift+Enter/arrow keys; CSV export of results; debounced live search (300ms); per-document text caching with invalidation on close/modify; AbortController cancellation on query change; 500 results/doc limit. Keyboard: Ctrl+Shift+F to open/close, Escape to close from anywhere. Integrated with: action registry (`view.cross-search`), command palette, event bus (`cross-search:*`), tabs.js (new `getTabPdfDocument`/`getAllTabDocuments` exports). New file: `js/cross-search.js`. Modified: `index.html`, `css/tools.css`, `js/tabs.js`, `js/action-registry.js`.
- **BUG FIX** — TASK-082: Fixed thumbnail context menu selector mismatch. `detectContext()` in `context-menu.js:432` used `.page-thumb, .page-thumbnail, [data-page-thumb]` but actual sidebar thumbnails are `.thumbnail-item[data-page]`. Added `.thumbnail-item` to the selector. Intentionally did NOT add the broad `[data-page]` selector to avoid false positives on `.pdf-page-wrapper`, minimap items, and other elements that also carry `data-page`. Verified: right-clicking thumbnail shows 10 correct menu items; right-clicking viewer area still shows blank-area menu.
- **NEW FEATURE** — TASK-115: Auto-scroll reader. Hands-free continuous scrolling for reading long PDFs at user-controlled speed (10–200 px/s with WPM estimate). Features: floating control panel with play/pause, speed slider, slower/faster buttons; collapses to minimal pill after 3s inactivity, expands on hover; thin progress bar at top of viewport; keyboard controls (Space=play/pause, arrows=speed, +/-=fine adjust, Esc=stop); click-to-pause on document; position/speed persistence per document in localStorage; auto-pause on tool switch, modal open, document change, presentation mode; auto-stop at end of document with toast. Integrated with: action registry (5 actions: toggle/start/stop/speed-up/speed-down), command palette, keyboard shortcuts (Shift+A), event bus (`auto-scroll:*`). New file: `js/auto-scroll.js`. Modified: `index.html` (toolbar button + script tag), `css/tools.css`, `js/action-registry.js`.

## 2026-04-08

- **NEW FEATURE** — TASK-114: Annotation reply threading. Adds threaded comment discussions on any annotation (highlights, sticky notes, stamps, drawings). Features: reply popover with nested threads (up to 3 levels), author management (localStorage-persisted), review status markers (None/Accepted/Rejected/Completed/Cancelled) with system messages logged in thread, reply count badges on annotation elements (green checkmark for resolved), inline reply/edit/delete, Ctrl+Enter to submit. Integrated with: event bus (`annotation:reply`, `annotation:reply-added`, `annotation:status-changed`), action registry (`annotate.reply`, `annotate.set-status`), command palette, context menu (existing "Reply / Add Comment" entry now wired). Session persistence via sessionStorage. Cascading delete when parent annotation removed. Public API exports for annotation-summary and annotation-exchange integration. New file: `js/annotation-replies.js`. Modified: `index.html`, `css/tools.css`.
- **NEW FEATURE** — TASK-113: Intelligent content-aware document splitting (Smart Split). Analyzes PDF text content to auto-detect natural document boundaries — blank pages (via low-res canvas rendering + average pixel brightness), chapter/section headings (via pdf.js `getTextContent()` font-size analysis, position detection, pattern matching for "Chapter N", uppercase titles, numbered sections), page number resets, and repeated header/footer filtering. Results shown in scrollable list with checkboxes, confidence indicators, and method icons. Sensitivity slider (Low/Medium/High) re-filters cached analysis without re-scanning. Manual split point addition. Output files named by detected heading text. ZIP download support via JSZip. Integrated with action registry (`split.smart-analyze`, `split.smart-execute`), event bus (`smart-split:analysis-complete`, `smart-split:executed`), and command palette. New file: `js/smart-split.js`. Modified: `index.html`, `css/tools.css`, `js/split.js`, `js/split-bookmarks.js`, `js/split-by-size.js`, `sw.js`.
- **NEW FEATURE** — TASK-112: Auto page rotation detection and correction. Analyzes PDF pages to identify incorrectly rotated pages (90°/180°/270°) using text-transform analysis from pdf.js `getTextContent()` with image-based edge-density heuristic fallback for scanned pages without text. Shows thumbnail grid with color-coded indicators (green=correct, orange=rotated, gray=uncertain) and a summary line. One-click "Auto-Fix All" applies corrections via pdf-lib `/Rotate` entry (non-destructive). Manual per-page override buttons. Integrated with: action registry (`rotation-detect.analyze`, `rotation-detect.fix-all`), command palette, undo/redo system, event bus (`pages:rotation-detected`, `pages:rotation-fixed`), batch processing. New file: `js/auto-rotate.js`. Modified: `index.html`, `css/tools.css`.

- **Bug fix**: Fixed TypeError in `measure.js` — `ensureSvgOverlay` crashed on null `viewport` during tool switching. Added null guards in both `onPageRendered` and `ensureSvgOverlay`. Verified with full 74-tab sweep: 0 console errors.

- **INFRASTRUCTURE** (supervisor) — Fixed Chrome/MCP memory leak in pipeline. 10 orphaned `chrome-devtools-mcp` processes (ppid=1) from old cycles were keeping Chrome browsers alive, consuming ~10GB RSS. Available RAM had dropped to 539Mi. Killed orphans, freed 4.7GB (recovered to 5.3Gi). Added cleanup block to `scripts/cron-orchestrator.sh` that runs at each cycle start: kills orphaned MCP processes and removes stale `/tmp/puppeteer_dev_chrome_profile-*` directories.

- **NEW FEATURE** — TASK-110: Callout text annotation tool. Text box with leader line (arrow) pointing to a specific location on the PDF page — the standard FreeText/FreeTextCallout annotation per PDF spec. Click on the page to set the arrow tip, drag to position the text box, then type the note. Features: configurable text styling (font family/size/color/bold/italic/alignment), background color with opacity, border styling, multiple arrowhead styles (open/closed/circle/diamond/square), drag-to-move text box, drag-to-repoint arrow tip, resize handles, inline contenteditable text editing, delete button. Keyboard shortcut Alt+C. Integrated with: undo/redo system (add/delete/move/edit commands), annotation summary (callouts listed with text and page), annotation exchange (XFDF export as FreeText with intent=FreeTextCallout and /CL callout line), action registry + command palette ("Callout"), pdf-lib PDF embedding (background rect, leader line, arrowhead, wrapped text). New file: `js/callout.js`. Modified: `index.html`, `css/tools.css`, `js/action-registry.js`, `js/annotate.js`, `js/annotation-summary.js`, `js/annotation-exchange.js`.

- **ECOSYSTEM** — Expanded the tester agent from a 2-phase to a **6-phase browser smoke test** after a user-reported regression revealed that the old smoke test was blind to layout bugs. Root cause of the regression: `js/reading-progress.js` inserted `.reading-progress-bar` as a direct child of `.pdf-viewer-container`, which at `viewer.css:1917` is overridden to `flex-direction: row !important` to accommodate the minimap sidebar. A row-flex sibling with `width: 100%; flex-shrink: 0` eats the entire row, leaving `.pdf-viewer-inner` and thus `#pdf-pages` with 0 width — the PDF loaded correctly (`totalPages === 1`, canvases created) but was invisible to the user. The old smoke test only checked `totalPages >= 1 && pdfDocLoaded === true`, so it passed. **Fix**: (a) patched `reading-progress.js` to insert the bar into `.pdf-viewer-inner` (column layout) instead of `.pdf-viewer-container`; (b) rewrote the tester's smoke test protocol into 6 phases — homepage console → upload → **post-upload geometry/visibility check** (`containerWidth >= 300`, `visibleCanvasCount >= 1`) → **tool-tab interaction sweep** (clicks 10 tool tabs, verifies panels activate, re-reads console) → **viewer interaction** (zoom/fit-width, geometry still sane) → cleanup. Every phase has an explicit failure prefix (`UPLOAD RENDER BROKEN`, `TOOL PANELS BROKEN`, `VIEWER LAYOUT BROKEN`) and files a SYSTEM CRITICAL entry with the raw diagnostic JSON. Tester is explicitly forbidden from fixing bugs itself. Also updated: PM prompt (SYSTEM CRITICAL jumps the queue; smoke-test-verdict-aware FAIL handling), developer + developer2 prompts (SYSTEM CRITICAL priority; mandatory end-to-end visibility check after touching viewer/layout code; explicit warning about inserting flex-row children into `.pdf-viewer-container`), supervisor prompt (monitors stale SYSTEM CRITICAL and tester prompt drift), `CLAUDE.md` tester row, `README.md` agents table. Files updated: `actors/tester/prompt.md`, `actors/project-manager/prompt.md`, `actors/developer/prompt.md`, `actors/developer2/prompt.md`, `actors/supervisor/prompt.md`, `CLAUDE.md`, `README.md`, `js/reading-progress.js`.

- **NEW FEATURE** — TASK-109: Recent files list. IndexedDB-backed recent file storage (up to 25 entries) with two object stores (metadata + file data). Auto-generates first-page thumbnails via pdf.js. Welcome screen shows recent files grid with thumbnails, file names, sizes, and relative timestamps. Slide-out panel with cache toggle (files cached for offline reopening), cache size display, search/filter (10+ files), sort options, per-card context menu (remove, copy name), and "Clear All" action. Reopening cached files is instant — loads from IndexedDB without re-upload. Reading position tracked per file. Integrated with command palette ("Recent Files", "Open Recent", "File History"), keyboard shortcut (Ctrl+Shift+R), and toolbar button (clock icon). Dark mode compatible. New file: `js/recent-files.js`. Modified: `css/tools.css`, `index.html`.

- **NEW FEATURE** — TASK-107: Custom stamp creator and stamp library manager. Full stamp designer with text-based and image-based stamp creation modes, multi-line text with dynamic placeholders ({DATE}, {TIME}, {DATETIME}, {USER}), configurable fonts/borders/backgrounds, canvas live preview. My Stamps library section (max 50 stamps, localStorage-persisted) with edit/duplicate/delete/export-as-PNG per stamp. Favorites system for both preset and custom stamps with quick-access bar. JSON import/export for sharing stamp libraries. Integrated with command palette ("Create Custom Stamp", "Manage Stamps", "Stamp Library"), context menu ("Place Custom Stamp" submenu), dark mode (CSS variables), and undo/redo system. New files: `js/custom-stamps.js`. Modified: `js/stamps.js`, `js/action-registry.js`, `js/context-menu.js`, `css/tools.css`, `index.html`.

- **BUG FIX** — Fixed 100+ JS errors on homepage (SYSTEM CRITICAL). Three root causes resolved: (1) Created `js/event-bus.js` to break circular dependency — `bus` was exported from `app.js` but 98 modules importing it hit TDZ because `app.js` hadn't finished evaluating; bus now lives in its own dependency-free module with `app.js` re-exporting it for backward compat. (2) Removed duplicate `export` of `getEnabled` in `js/context-menu.js` (was exported both inline at line 27 and in the aggregated export list at line 745). (3) Added missing `<script src="lib/pdf-lib.umd.js"></script>` to `index.html` before module scripts — the ESM shim at `lib/pdf-lib.min.js` requires `window.PDFLib` to be populated first.

- **VERIFIED** — TASK-081: Document Statistics panel confirmed working after homepage blocker resolved. All 7 sections render correctly (Overview, Text Stats, Font Inventory, Image Analysis, Structure, File Size Breakdown, Page Breakdown). Fixed stale pdf-lib import path (`./lib/` → `../lib/`) that caused a 404. JSON/CSV export functional.

- **BUG FIX** — TASK-108: Fixed `layers.js` unhandled promise rejection on PDFs without Optional Content Groups. Added null/type guard around `getGroups()` call and Map→object conversion in `parseConfig()`. Console errors reduced to 0 on example.pdf load.

- **BUG FIX** (supervisor) — Developer's SYSTEM CRITICAL fix was incomplete: `event-bus.js` was created and `app.js` re-exported `bus`, but all 100 consumer modules still imported `{ bus }` from `./app.js`. The re-export doesn't break the TDZ in circular dependency resolution — Chrome resolves the binding through `app.js` which hasn't finished evaluating. **Real fix**: rewired all 100 consumer modules to `import { bus } from './event-bus.js'` directly, splitting `import { bus, state } from './app.js'` into two separate import lines. Verified 0 console errors via chrome-devtools MCP.

- **INFRASTRUCTURE** — `chrome-devtools` MCP server installed at user scope (`~/.claude.json`) and wired into the **tester** agent. Headless Chrome runs sandboxed on the VPS after enabling unprivileged user namespaces (`/etc/sysctl.d/60-chrome-sandbox.conf`: `kernel.apparmor_restrict_unprivileged_userns=0`). Tester now performs a mandatory **Browser Smoke Test** every run: opens https://cronloop.techtools.cz/ in headless Chrome, classifies all console messages, fails the run on any app-origin error, and uploads `/home/novakj/test-fixtures/example.pdf` via `mcp__chrome-devtools__upload_file` to verify the upload pipeline end-to-end. App-origin errors raise a `SYSTEM CRITICAL` entry in `tasks.md` so the developer/PM agents pick up the fix on the next cron tick. Updated: `actors/tester/prompt.md`, `CLAUDE.md`, `README.md`, `docs/autonomous-system.md`. Initial run against current production found **99 console errors** (1× `bus` TDZ from circular import in `app.js`/`command-palette.js` cascading across ~91 modules, 1× UMD/ESM mismatch in `lib/pdf-lib.min.js`, and a duplicate-export SyntaxError) — these will be flagged by the next tester cron tick.

- [TASK-106] **NEW FEATURE** — Find and Replace text across PDF documents. New module `js/find-replace.js` extends the existing search bar with replace functionality. Features: toggle replace mode via Ctrl+H, regex support with capture group references ($1, $2), Replace current match and Replace All buttons, preview dialog for Replace All showing all matches with context and checkboxes to exclude individual matches, batch undo/redo support (Replace All is a single undo step via BatchCommand), replaced matches highlighted in green, per-match cover-and-redraw via pdf-lib on PDF download, case-sensitive/whole-word/regex toggles. Context menu integration: "Find and Replace" option in text selection menu pre-fills the search query. Registered in command palette as "Find and Replace", "Search and Replace", "Replace Text". CSS in viewer.css with dark mode support. All client-side — no data leaves the browser.

- [TASK-105] **NEW FEATURE** — Multi-page selection with batch page operations. Enhanced `js/pages.js` with: **Batch operations toolbar** — floating bar appears when 2+ pages selected with Rotate CW/CCW, Delete, Extract, Copy, Move To buttons showing dynamic counts. **Extract** — extracts selected pages to a new downloadable PDF using pdf-lib. **Copy/Duplicate** — duplicates selected pages and inserts after last selected. **Move To** — modal dialog to move selected pages to a specific position. **Delete confirmation** — confirmation dialog when deleting 2+ pages. **Multi-page drag-and-drop** — dragging a selected page moves all selected pages together with a ghost count badge. **Right-click context menu** — batch-aware options (Rotate/Delete/Duplicate/Extract/Move N Pages) integrated with context-menu.js. **Keyboard shortcuts** — Ctrl+Shift+R (rotate CW), Ctrl+Shift+E (extract), Escape (deselect). **Visual feedback** — green flash animation on affected pages after operations. **Shift+Click** — improved range select using last-clicked index tracking. All CSS with responsive support and dark mode compatibility. All client-side.

- [TASK-104] **NEW FEATURE** — Open PDF from URL. New module `js/open-url.js` adds an "Open from URL" feature allowing users to paste a web URL to fetch and load a PDF directly in the editor. Features: URL validation (HTTP/HTTPS only, rejects javascript:/data:/file: protocols), download progress bar with speed and ETA display, indeterminate progress for unknown content lengths, PDF magic bytes validation, Content-Disposition filename extraction, URL path-based filename fallback, rate limiting (5 fetches/min), 30s timeout with AbortController, CORS error handling with helpful user guidance, URL history (last 10 URLs in localStorage), drag-and-drop URL support on the dropzone. Registered in command palette as "Open PDF from URL" with `Ctrl+Shift+U` shortcut. Link icon button added to header toolbar. Fully accessible modal dialog with focus trap, keyboard navigation, and aria-live progress updates. All client-side — no data leaves the browser.

- [TASK-290] **NEW FEATURE** — Export PDF Pages as SVG. New module `js/svg-export.js` adds "To SVG" sub-tab in the Convert panel. Exports PDF pages as scalable vector graphics with two text modes: "Selectable" (overlays transparent `<text>` elements from pdf.js text extraction for searchability in vector editors) and "Outlined" (visual fidelity only, no text overlay). Features: page selection (current/all/custom range), configurable render quality (1x/2x/3x scale), SVG optimization toggle, live preview, progress bar with aria-live announcements. Multi-page exports bundled into ZIP via JSZip. Registered in action registry as `convert.svg` with keyboard shortcut `Ctrl+Shift+V`. Accessible with proper ARIA labels and keyboard navigation. All client-side.

- [TASK-101] **NEW FEATURE** — Customizable Toolbar and Workspace Layout Manager. New module `js/toolbar-manager.js` enables users to rearrange, hide/show, and group toolbar tools to match their workflow. **Drag-and-drop reordering**: In customize mode (activated via wrench icon button or `Ctrl+Shift+T`), each tool button becomes draggable with visual insertion indicators. Drag tools to reorder, click the X badge to hide tools. **Workspace presets**: Three built-in presets — "Edit" (all tools visible, default), "Review" (annotations/redaction/signatures first), and "Read" (minimal with just viewer, reader, OCR, convert, statistics). Preset switcher dropdown in the header for instant switching. **Custom presets**: Users can save their current toolbar arrangement as a named preset and switch between them. **Overflow palette**: Hidden tools appear in a dropdown "More tools" panel with one-click restore. **Persistence**: All settings stored in localStorage. **Responsive**: On narrow viewports (<768px), toolbar auto-collapses to top-6 most-used tools (tracked by usage count). **Keyboard support**: In customize mode, arrow keys to move tools, Delete to hide. **Command palette integration**: Registered actions for "Customize Toolbar", "Switch Workspace: Edit/Review/Read", and "Reset Toolbar" in the Settings category. **CSS**: Full dark/light mode support with matching styles for separators, customize overlay panel, preset buttons, overflow palette. All client-side.

## 2026-04-07

- [TASK-100] **NEW FEATURE** — Inline Text Editing. New module `js/text-edit.js` enables users to click on existing PDF text and edit it directly in place. **How it works**: "Edit Text" button added to the annotation toolbar; pressing it (or keyboard shortcut `E`) activates text editing mode. The module uses pdf.js `getTextContent()` to extract all text items on the page with their positions, font names, and sizes, then groups adjacent items into logical text blocks by Y-coordinate proximity. Each text block is rendered as an interactive hover-highlighted overlay. Clicking a block opens an inline editor with a mini-toolbar (font family, size, color, confirm/cancel buttons) and a textarea pre-filled with the original text, matching the font appearance. **PDF modification**: Uses the industry-standard cover-and-redraw approach via pdf-lib — draws a white rectangle over the original text area, then draws the new text in the same position with matched font, size, and color. Supports Helvetica/Times/Courier font families with bold/italic variants. **Integration**: Integrated with the annotate.js save flow so text edits are applied when downloading the PDF. Registered in the command palette as "Edit Text Mode". Info banner appears when mode is active. **Keyboard shortcuts**: `E` to toggle edit mode, `Enter` to confirm edit, `Escape` to cancel, `Shift+Enter` for newlines. Dark mode compatible. Accessible with aria-live announcements. All client-side.

- [TASK-099] **NEW FEATURE** — Before/After Comparison Slider for PDF page operations. New module `js/comparison-slider.js` provides an interactive overlay for visually comparing original and modified PDF pages during destructive/transformative operations. **Three comparison modes**: (1) Slide — draggable vertical divider that reveals original on right and modified on left, with a circular grip handle, keyboard arrow key control (Shift for 10% jumps), and real-time percentage display; (2) Side by Side — original and modified rendered next to each other with labels; (3) Toggle — click to alternate between versions, with optional blink auto-toggle for spotting subtle differences. **Difference highlighting**: toggleable pixel-diff overlay that computes per-pixel Euclidean color distance and highlights changed regions in red with configurable threshold. **Opacity blend**: slider to cross-fade between original and modified in slide mode. **Multi-page navigation**: when operations affect multiple pages, navigate between them within the comparison view. **Event-driven architecture**: other modules emit `comparison:show` with original/modified canvases and operation name; exports `showQuickComparison()` and `renderPageToCanvas()` helpers for easy integration. **Keyboard shortcuts**: 1/2/3 to switch modes, D for diff toggle, C or Esc to close, arrow keys for slider/page navigation. **Command palette**: registered as "Toggle Before/After Comparison" plus mode-specific actions. **Responsive CSS**: full-screen overlay with toolbar, canvas area, status bar; mobile-friendly with reflowing toolbar. Dark mode compatible using CSS custom properties. All client-side.

- [TASK-074] **BUG FIX** — Fixed all 8 issues in the Background & Borders module (`js/bg-borders.js`). Critical fixes: SVG backgrounds now convert to PNG before PDF embedding instead of silently failing; gradient PDF output rewritten to render via canvas-to-PNG for full radial gradient and arbitrary angle support (was only horizontal/vertical). New features: command palette integration (3 actions), custom preset save/load/delete via localStorage, before/after preview toggle with original page overlay, background detection and removal scanning first content stream for full-page rect fills, and added missing "circles" pattern type to HTML, preview, and PDF output.

- [TASK-098] **NEW FEATURE** — Sticky Page Tab Markers. New module `js/page-tabs.js` adds colored edge tabs to PDF pages for quick visual navigation and organization. **Tab types**: color tabs (8 colors with accessibility patterns), labeled tabs (auto-sized with text up to 15 chars), and icon tabs (checkmark, star, flag, question, exclamation, bookmark, pen, eye). **Placement**: click-to-place on right/left/top/bottom page edges with snap-to-grid (20 slots) and Alt for free positioning, live placement preview follows cursor. **Rendering**: semi-transparent canvas overlay with shadows, rounded corners, accessibility patterns for colorblind users, hover expansion. **Navigator panel**: new sidebar tab with summary bar, color breakdown, filter chips by color, text search, sort by page/date, tab list with click-to-navigate. **Thumbnail integration**: colored markers on thumbnail edges matching tab positions. **Batch operations**: add to all pages, add to page range, export/import as JSON, remove by color, clear all, convert tabs to bookmarks. **Persistence**: localStorage keyed by file fingerprint, auto-restore on reopen. **Keyboard shortcuts**: T toggle tool, 1-8 select color, Delete remove selected, Ctrl+Shift+T open navigator. **Context menu**: right-click tabs for edit/color/move/delete. **Integration**: command palette actions, reading progress auto-tab, bookmark conversion. All client-side, no PDF modification.

- [TASK-096] **NEW FEATURE** — PDF Repair & Recovery tool. New module `js/repair.js` detects and fixes common structural issues in damaged/corrupted PDFs. **Damage detection engine**: analyzes PDF header, %%EOF marker, cross-reference table (xref), trailer dictionary, object structure, page tree, stream lengths, broken object references, encryption, linearization, and missing MediaBox entries. **Repair engine**: uses pdf-lib with lenient loading options to re-save and fix structural issues; includes multi-pass loading (standard, throwOnInvalidObject:false) and a last-resort rebuild-from-scratch mode that attempts to extract page content from severely damaged files. **Validation mode**: health check for working PDFs — scores structural integrity 0-100 based on header, EOF, metadata, font embedding, page structure, JavaScript presence, and incremental updates. Animated score ring visualization. **UI**: tool panel with Repair/Validate mode tabs, drag-and-drop file upload for damaged PDFs, "Use Current File" option, progress bar, structured issue report with severity badges (Critical/Warning/Info) and color-coded issue cards, download repaired PDF button, and JSON repair report export. **Integration**: nav tab with wrench icon, command palette entries for "Repair PDF" and "Validate PDF", action registry with mode switching. Full CSS in tools.css with responsive layout and dark mode support.

- [TASK-072] **BUG FIX** — Font Inspector: fixed all 5 tester-reported issues. (1) **Font embedding fix** — added two repair strategies: "Substitute with standard font" maps non-embedded fonts to PDF standard fonts (Helvetica, Times, Courier families) via pdf-lib embedFont; "Render and replace" renders affected pages as high-res PNG images and overlays them to preserve appearance. Includes font substitution mapping for 30+ common fonts. (2) **Character map glyph preview** — renders a 16-column grid of the first 200 available glyphs (U+0020–U+00E7) on a canvas, with CSS font approximation based on font flags and name. (3) **Sample text preview** — displays "The quick brown fox..." and full alphabet in the approximated font family with correct bold/italic styling. (4) **Duplicate font name detection** — post-scan analysis groups fonts by base name, compares type/encoding/embedding/glyph count, and adds DUPLICATE_FONT_NAME warning issues when properties differ. (5) **PDF font report export** — generates a formatted A4 PDF report via pdf-lib with title, health summary, PDF/A compliance, full font inventory with color-coded status, and issues summary. Added new CSS styles for glyph grid, sample text, and fix section UI.

- [TASK-093] **NEW FEATURE** — Reader Mode with content reflow. New module `js/reader.js` provides a distraction-free reflowable text view for comfortable reading on any screen size. **Text extraction engine** reuses markdown-export structural analysis: heading detection (font size ratios), paragraph grouping (vertical gap analysis), list detection (bullet/number patterns), bold/italic/monospace detection (font name parsing), multi-column layout handling. Renders extracted text as semantic HTML (`<h1>`-`<h4>`, `<p>`, `<ul>`/`<ol>`, `<strong>`/`<em>`, `<code>`). **Reader panel** overlays the PDF canvas with a clean, centered content column. **Reading customization toolbar**: font size (14-28px with A-/A+ controls), font family (serif/sans/mono), theme (auto/light/sepia/dark), line spacing (1.4/1.6/1.8/2.0), column width (narrow/medium/wide/full). All preferences saved to localStorage. **Two reading modes**: paginated (with prev/next buttons and swipe gestures) and continuous scroll (with page dividers and lazy loading via IntersectionObserver). Auto-selects paginated on mobile, continuous on desktop. **Progress tracking**: thin progress bar at top, page indicator, estimated reading time (~238 WPM), reading position persistence per document. **Keyboard navigation**: arrow/PageUp/PageDown for paginated, +/- for font size, Escape to exit. **Touch support**: swipe left/right for page navigation in paginated mode. **RTL text detection** for Arabic/Hebrew content. **Performance**: per-page text cache, batch extraction (20 pages at a time) with progress indicator, lazy loading in continuous mode. **Copy all text** button for clipboard export. Added nav tab with book icon, reader panel HTML, and comprehensive CSS with responsive breakpoints.

- [TASK-092] **NEW FEATURE** — Touch and gesture optimization for tablet and mobile PDF editing. New module `js/touch.js` adds comprehensive touch support: **Pinch-to-zoom** with midpoint anchoring and real-time CSS transform feedback, re-rendering at final zoom on touchend. **Swipe navigation** with velocity-based detection for single/dual page modes. **Double-tap** toggles between fit-page and 150% zoom. **Long-press** triggers context menu with haptic vibrate feedback. **Pan scrolling** when zoomed in with inertial deceleration. **Annotation mode** awareness — single-finger draws, two-finger scrolls. **Responsive CSS** breakpoints for mobile (<768px), tablet (768-1024px), desktop — nav tabs become scrollable row, sidebar becomes bottom sheet with drag handle, minimum 44x44px tap targets. **Orientation handling** with auto re-render. **Online/offline indicator** (cloud icon). **Virtual keyboard handling** via visualViewport API. **Gesture help overlay** on first touch-device visit. **Pull-to-refresh prevention**. Added to service worker precache. Viewport meta updated with viewport-fit=cover for notched devices.

- [SECURITY] Security review — 4 new PWA files (pwa.js, sw.js, manifest.json, offline.html) + modified tts.js (TASK-068). All reviewed and secure. No critical or medium issues found. New finding: offline.html inline onclick handler blocked by CSP (functional bug, not security). JS count: 90 files, 326 innerHTML usages — all safe. fail2ban: 8821 total bans (+49). SSL valid 72 days.

## 2026-04-06

- [TASK-088] **NEW FEATURE** — Annotation style presets and quick-apply palette. New module `js/annotation-presets.js` adds a complete preset system for annotation styles. **Preset Manager**: Modal panel with CRUD operations for presets — each stores annotation type (highlight/underline/strikethrough), fill color, opacity, and keyboard shortcut (1-9). 8 built-in defaults: Error (red), Question (yellow), Approved (green), Important (orange underline), Reference (blue underline), Comment (purple), Redline (red strikethrough), Draft Note (gray). Users can edit, duplicate, delete, reorder via drag-and-drop. **Quick-Apply Palette**: Floating toolbar appears when annotation mode is active, showing preset swatches with type icons. Click to activate a preset (sets both tool type and color). Active preset shown with border ring. Draggable with position persistence. Collapsible to save space. **Style Inheritance**: Active preset color flows to annotation creation. Modified indicator when user changes style while preset is active, with "Update" option. **Import/Export**: Export presets as JSON file, import from JSON with validation and sanitization. Enables team style guide sharing. **Keyboard Shortcuts**: Keys 1-9 activate presets in annotation mode (scoped to avoid conflicts). **Integration**: Registered in command palette as "Manage Annotation Presets", "Export/Import Style Presets". Context menu items exposed for "Apply Preset" submenu. "Presets" button added to annotation toolbar. All presets persisted in localStorage. Respects dark/light/sepia themes via CSS custom properties.

- [TASK-087] **NEW FEATURE** — PDF page interleave and collate tool. New module `js/interleave.js` adds a page interleave/collate tool for reconstructing correctly-ordered documents from separately scanned front and back pages. **Dual-file mode**: Two drop zones for odd/front pages (File A) and even/back pages (File B), interleaved into a single correctly-ordered PDF. **Reverse-even mode** (default): Reverses the second file before interleaving — the most common real-world scenario when back pages are scanned last-to-first. **Standard mode**: Simple 1:1 alternating interleave. **Custom pattern mode**: User-defined interleave pattern (e.g., 2:1 = take 2 from A, 1 from B). **Single-file mode**: Split a single PDF at a configurable midpoint and interleave the two halves — for documents where all fronts then all backs were scanned into one file. **Live preview**: Shows first 20 result pages as color-coded thumbnails (blue for File A, green for File B) with page source labels. Handles unequal page counts by appending remaining pages. **Implementation**: Uses pdf-lib `copyPages()` and `addPage()` with batched processing (50 pages per batch with UI yielding) for large documents. **UI**: New "Interleave" nav tab between Split and Pages, full tool panel with dropzones, mode radio buttons, preview grid, and summary. **Integration**: Registered in command palette as "Interleave Pages", "Collate Scanned Pages", "Merge Front and Back Pages". CSS added to tools.css. All processing client-side using pdf-lib.

- [TASK-065] **BUG FIX / ENHANCEMENT** — Digital signature verification: fixed all 6 tester-reported issues. **(1) Asymmetric cryptographic verification**: Now uses `crypto.subtle.importKey()` to import the signer's public key from the X.509 certificate's SPKI, and `crypto.subtle.verify()` to cryptographically verify the CMS signature against the signer's public key. Supports RSA (PKCS#1 v1.5) and ECDSA (with DER-to-P1363 signature conversion). The signed attributes are re-encoded with SET tag (0x31) per CMS spec before verification. **(2) Signed version viewer**: "View Signed Version" button on each signature truncates the PDF to the signature's byte range and opens it in a new tab, showing exactly what each signer saw and approved. **(3) Batch verification**: "Batch Verify Multiple PDFs" button opens a modal with file upload dropzone; verifies signatures across multiple PDFs with full crypto verification, displays results in a summary table, and supports CSV export. **(4) Self-signed certificate detection**: Compares subject and issuer DN fields; flags self-signed certificates with a warning badge and downgrades status from "valid" to "warning (untrusted)". **(5) SHA-1/MD5 weak algorithm handling**: SHA-1 triggers a deprecation warning; MD5 returns an explicit "unsupported algorithm" error instead of silently falling through to SHA-256. **(6) Dead code removed**: `panelEl`, `gapSize`/`coveredPlusGap`, and `sigVal` variables eliminated. Also added EC curve extraction from certificate SPKI parameters, and stored digest/signature algorithm OIDs for Web Crypto parameter resolution.

- [TASK-086] **NEW FEATURE** — PDF page minimap and document overview strip. New module `js/minimap.js` adds a collapsible sidebar on the right edge of the PDF viewer for fast navigation in long documents. **Minimap strip**: Renders low-resolution thumbnails (0.18 scale) of all pages in a vertical strip. **Lazy rendering**: Uses IntersectionObserver to only render thumbnails as they scroll into view within the minimap, with a 200-page cache limit. **Viewport indicator**: Semi-transparent blue rectangle on the minimap shows the current viewport position, updated in real-time during scrolling via requestAnimationFrame. **Click-to-navigate**: Clicking a thumbnail scrolls to that page; clicking a specific position within a thumbnail scrolls to that exact vertical position. **Drag navigation**: The viewport indicator can be grabbed and dragged to smoothly scroll through the document. **Hover preview**: Hovering over a thumbnail shows a larger tooltip preview (0.45 scale) with page number. **Resize and collapse**: Minimap width is adjustable via drag handle (50-200px range), collapsible via toolbar button, preferences persisted in localStorage. **Responsive**: Auto-hides on screens narrower than 768px. **Markers**: Listens for search:results and annotations:updated events to show colored indicator dots on minimap thumbnails. **Integration**: Toggle button in viewer toolbar, registered in command palette as "Toggle Minimap", "Show Document Overview", "Page Minimap". Dark mode compatible. HTML restructured with pdf-viewer-inner wrapper to support flex-row layout with minimap sidebar. All client-side.

- [TASK-085] **NEW FEATURE** — Smart pattern-based redaction (PII auto-detection). New modules `js/smart-redact.js` and `js/smart-redact-worker.js` add intelligent PII scanning to the PDF editor. **Pattern detection engine**: Client-side regex-based scanner running in a Web Worker, detects 8 built-in PII categories: SSN (with format validation), credit cards (Luhn algorithm + card type identification), email addresses, phone numbers (US/international formats), dates of birth (keyword-contextual), IP addresses, bank account/routing numbers, and passport numbers. **Custom patterns**: Users can define regex patterns with labels, saved to localStorage for reuse. **Scanning workflow**: Extracts text via pdf.js `getTextContent()`, maps matches back to page coordinates using text item transforms, supports scanning current page or all pages. **Results panel**: Categorized sidebar with color-coded groups, match counts per category, partially masked previews (e.g., `***-**-1234`), confidence indicators (High/Medium), click-to-navigate to match location with flash animation. **Selection controls**: Individual match checkboxes, category-level select/deselect, Select All/Deselect All buttons. **Mark as Safe**: Per-document ignore list for false positives. **Visual overlay**: Semi-transparent colored rectangles over detected matches on the PDF canvas, different colors per category, hover tooltips, click to toggle selection. **Redaction**: Uses existing pdf-lib page flattening approach — renders pages to canvas, draws opaque rectangles over selected regions, downloads redacted copy preserving original. Confirmation dialog warns about irreversibility. **Performance**: Web Worker for non-blocking pattern scanning, progress bar for multi-page documents. **Integration**: Nav tab with shield icon, registered in command palette as "Smart Redact", "Find Sensitive Data", "Auto-detect PII". Added to right-click context menu as "Scan Page for Sensitive Data". PDF viewer visible when tool active. Dark mode compatible. All processing entirely client-side.

- [TASK-083] **NEW FEATURE** — Auto-crop whitespace margins tool. New module `js/auto-crop.js` automatically detects content boundaries on PDF pages and trims excess whitespace. **Content detection**: Renders pages to canvas at 150 DPI via pdf.js, scans pixel data edge-inward to find non-background content bounding box. **Background detection**: Samples corner pixels and edge midpoints, uses median color to handle off-white/yellowish scanned documents. **User controls**: Dialog with scope (current page, all pages, page range), margin padding in mm (default 5mm), tolerance slider (strict to aggressive), uniform crop option for consistent margins across pages. **Preview**: Shows red dashed crop lines overlaid on the page with dimmed outside area and dimension labels. **Batch processing**: Processes pages in batches of 5 with `requestIdleCallback` between batches to keep UI responsive, shows progress bar. **Non-destructive**: Sets CropBox only, preserves MediaBox for undo. Handles rotated pages (90/270 axis swap). Skips blank pages with warning. **Integration**: Registered in command palette as "Auto-Crop Margins", "Trim Whitespace", "Remove Margins". Added to page thumbnail right-click context menu as "Auto-Crop This Page". Button in pages toolbar next to Crop. All client-side processing.
- [TASK-082] **NEW FEATURE** — Context-sensitive right-click menu system. New module `js/context-menu.js` provides a comprehensive right-click context menu that adapts to what the user clicks on. **8 context zones**: (1) Text selection — copy, highlight with color submenu (yellow/green/blue/pink), underline, strikethrough, sticky note, search, read aloud. (2) Annotation — edit, change color submenu (6 colors), delete, copy, reply, export. (3) Page thumbnail — rotate left/right, delete, duplicate, insert blank page before/after submenu, extract as PDF, crop, add page number. (4) Image — extract, copy, replace, view details. (5) Form field — edit properties, clear, copy value, tab order navigation. (6) Blank area — add text box, sticky note, stamp submenu (approved/rejected/draft/confidential/final/custom), paste, freehand drawing, insert image, zoom in/out, fit page/width. (7) Link — open, edit, copy URL, remove. (8) Tab bar — close, close others, close all, duplicate, copy filename. **Architecture**: Singleton menu container reused per invocation, lazy DOM construction, submenu support with viewport-aware positioning (flips horizontally/vertically). **Keyboard navigation**: Arrow keys, Enter to activate, Escape to close, Right/Left for submenu navigation. **Accessibility**: ARIA `role="menu"`, `role="menuitem"`, `aria-haspopup` for submenus, `aria-live` announcements, focus management. **User preference**: Toggle via command palette "Toggle Context Menu" action, Shift+Right-click always shows native browser menu. **Visual**: Themed with CSS custom properties, works with dark/light/sepia themes, SVG icons, shortcut hints, smooth fade-in animation. All client-side.

- [TASK-058] **VERIFIED** — PDF comparison tool. Verified all core requirements: four comparison modes (side-by-side, visual pixel diff, overlay blend, text diff with LCS), difference navigation with keyboard shortcuts, thumbnail strip with similarity indicators, full-page scan, HTML/JSON export reports, synchronized scrolling, zoom controls, drag-and-drop upload with validation. All files serve correctly, no security issues.

- [TASK-079] **NEW FEATURE** — Multi-select annotation management. New module `js/multiselect.js` enables selecting, moving, styling, and organizing multiple annotations at once. **Selection mechanics**: (1) Rubber-band selection — click and drag on empty space to draw a selection rectangle; annotations intersecting the rectangle are selected. (2) Ctrl/Cmd+click to toggle individual annotations in/out of selection. (3) Ctrl+A selects all annotations on current page, Ctrl+Shift+A selects all in document. **Filter-based selection**: Select by type (highlights, stamps, drawings, sticky notes) or by color via command palette actions. **Group bounding box**: When 2+ annotations are selected, a dashed blue bounding box appears around the group. Drag the box to move all selected stamps and sticky notes together, maintaining relative positions. **Bulk operations**: Right-click context menu with Delete, Duplicate, Change Color (10-color picker), and Deselect All. **Alignment tools**: Align Left/Center/Right/Top/Middle/Bottom for 2+ movable annotations. Distribute Horizontally/Vertically for 3+ annotations — evenly spaces them. **Named groups**: Group selected annotations under a name, ungroup to dissolve. **Keyboard shortcuts**: Delete/Backspace to delete selected, Ctrl+D to duplicate, Ctrl+G to group, Ctrl+Shift+G to ungroup, Escape to deselect, Arrow keys to nudge (1pt, Shift+Arrow for 10pt). **Undo integration**: All bulk operations (delete, move, nudge, color change, opacity change, align, distribute) recorded as single BatchCommand entries for atomic undo/redo via Ctrl+Z. **Command palette**: 18 actions registered — Select All, Select by Type (highlights, stamps, notes, drawings), Deselect, Delete Selected, Duplicate, Group/Ungroup, 6 Align directions, 2 Distribute directions. **CSS**: Selected annotations show blue outline with glow, rubber-band has dashed blue border, context menu with dark mode support, floating selection count badge. All client-side.

- [TASK-077] **NEW FEATURE** — PDF/A compliance checker and converter. New module `js/pdfa.js` validates PDFs against PDF/A standards (PDF/A-1b, PDF/A-2b, PDF/A-3b) and converts non-compliant documents. **Validation**: Checks 7 criteria — embedded fonts, color spaces (ICC profiles), transparency, JavaScript/actions, encryption, XMP metadata, and file attachments. Each criterion shows pass/fail/warning with detailed explanations and auto-fix availability. Circular score gauge shows overall compliance percentage. **Conversion**: Auto-fixes include stripping JavaScript and forbidden actions, adding XMP metadata with required fields (dc:title, dc:creator, xmp:CreateDate, pdf:Producer, pdfaid:part/conformance), embedding sRGB output intent for color compliance, and adding PDF/A identification. Re-validates after conversion. **UI**: "PDF/A" tab in main toolbar with shield/checkmark icon, tool panel with level selector dropdown, compliance results list, convert button, and download button. PDF/A badge indicator in panel header. **Integration**: Registered in command palette as "PDF/A Compliance" (Ctrl+Shift+A shortcut). Accessible with ARIA roles and live region announcements. All client-side, no server processing.

- [TASK-078] **NEW FEATURE** — Annotation exchange (XFDF export/import). New module `js/annotation-exchange.js` enables exporting and importing annotations as XFDF files for cross-tool collaboration with Acrobat, Foxit, and other PDF tools. **Export**: Collects all annotation types (highlights, underlines, strikethroughs, sticky notes, freehand drawings, shapes, stamps, hyperlinks, text overlays) and serializes them to XFDF 3.0 XML format with proper coordinate conversion (normalized 0-1 to PDF points), quadpoints for text markups, ink gesture lists for pen drawings, and action elements for links. Export panel provides type filtering checkboxes, page range input, live annotation count summary, and one-click XFDF download. **Import**: Parses XFDF files via DOMParser, reconstructs annotation objects from XML elements back into the app's internal data structures, and re-renders the viewer. Import preview dialog shows annotation counts per type with selective import checkboxes and merge mode selection (add all or replace existing on affected pages). Handles all standard XFDF elements: highlight, underline, strikeout, text (sticky notes), ink, square, circle, line (with arrow detection), stamp, link (URI and GoTo actions), and freetext. **Integration**: Export/Import buttons in annotation toolbar, registered in command palette as "Export Annotations (XFDF)" and "Import Annotations (XFDF)" with Ctrl+Shift+E / Ctrl+Shift+I shortcuts. Full CSS for modal dialogs with responsive mobile support. All processing client-side.

## 2026-04-05

- [TASK-075] **NEW FEATURE** — Clipboard paste support. New module `js/clipboard.js` enables pasting images and text from the system clipboard directly onto PDF pages using Ctrl+V / Cmd+V. **Image paste**: Detects image items in clipboard data (PNG, JPG, WebP, BMP), auto-converts to PNG, auto-downscales images >2000px on longest edge, places as a draggable/resizable overlay centered on the current page at 50% max dimensions. **Text paste**: Pastes plain text as positioned text overlay with auto-sized text box. Truncates at 10,000 chars. Ctrl+Shift+V forces plain text paste. **Disambiguation**: When clipboard contains both image and text, shows a floating menu to choose paste type with 5-second auto-dismiss defaulting to image. **Overlay controls**: Each pasted item has drag-to-reposition, corner resize handle (aspect-ratio-locked for images, free for text), delete button, apply button (embeds into PDF via pdf-lib), numbered badge, and arrow-key nudging (1pt/10pt with Shift). **Apply**: Individual or batch-apply embeds content into the PDF using pdf-lib's `drawImage()` and `drawText()`, updates the in-memory PDF. **Context menu**: Right-click on PDF page shows "Paste" and "Apply All" options. Uses async Clipboard API with fallback to paste event. **Command palette**: Registered as "Paste from Clipboard", "Paste Image", "Paste Text", "Apply All Pasted Items". **First-use hint**: Shows a one-time tooltip on first PDF load. All processing client-side.

- [TASK-073] **NEW FEATURE** — PDF to Markdown and plain text export. New module `js/markdown-export.js` adds a "To MD/TXT" sub-tab in the Convert panel. **Text extraction and structural analysis**: Uses pdf.js `page.getTextContent()` to extract text items with position, font size, and font name. Reconstructs document structure with heading detection (font size ratio thresholds: 2x=H1, 1.7x=H2, 1.4x=H3, 1.2x=H4 relative to mode body font size), paragraph grouping (Y-gap analysis), bullet/numbered list detection (Unicode bullets + numbered patterns), table detection (column-aligned text clustering), bold/italic detection (font name parsing), code block detection (monospace fonts), link extraction (annotation matching to text items), and image placeholders (operator list scanning). **Multi-column support**: Detects bimodal X-position distributions and extracts left-then-right column order. **Header/footer stripping**: Auto-detects text repeated on >50% of pages at top/bottom positions. **Output formats**: Markdown with YAML frontmatter (title, author, date, source, pages) and GFM tables/emphasis/code blocks, or plain text with structural hints (ALL CAPS headings, fixed-width tables, underscore emphasis). **Customization**: Page range selection, toggle heading/list/table/link detection, image handling (placeholders or omit), line wrapping (none/80/120 chars), page separator (horizontal rule/blank lines/none). **Live preview**: Raw text and rendered HTML preview (limited to 20 pages for performance). **Copy to clipboard** alongside download. **Smart cleanup**: Removes soft hyphens, normalizes Unicode quotes/dashes, fixes ligatures (fi/fl), collapses excessive blank lines, joins hyphenated line breaks. Progress bar with per-page updates and UI-responsive batching.

- [TASK-053] **BUG FIX** — Fixed bookmark detection in accessibility checker. The `PDFName` class was not imported from pdf-lib, causing `PDFDocument.PDFName` to be `undefined`. This meant `catalog.get()` received a plain string instead of a `PDFName` object, so bookmarks were never detected — any document >5 pages always got a false "Missing Bookmarks" failure. Fix: imported `PDFName` directly from pdf-lib and used `PDFName.of('Outlines')` for the catalog lookup.

- [TASK-072] **NEW FEATURE** — PDF font inspector and embedding tool. New module `js/font-inspector.js` adds a "Fonts" tab in the toolbar that scans all fonts used in a PDF document and presents a comprehensive analysis. **Font extraction**: Uses pdf.js `getOperatorList()` and internal `commonObjs` to collect font references from every page, extracting font name, type (Type1, TrueType, CIDFont, Type3, etc.), encoding, embedding status (embedded/subset/not-embedded), glyph count, font flags (bold, italic, serif, symbolic, etc.), ToUnicode mapping presence, and pages used. Detects subset prefixes (6-letter uppercase + "+"). **Font health dashboard**: Summary with color-coded health indicator (green = all embedded, yellow = subsets only, red = not embedded), per-status counts, and overall font count. **PDF/A compliance check**: Reports pass/fail for PDF/A-1b/2b/3b font embedding requirements with list of non-compliant fonts. **Issue detection**: Flags 6 issue types — not-embedded fonts (error), missing ToUnicode maps (warning), Type 3 bitmap fonts (info), symbolic fonts with non-standard encoding (info), minimal subsets (info), and duplicate font names. Issues sorted by severity. **Font table**: Searchable, filterable list of all fonts with expandable detail rows showing full properties, per-font issues, and clickable page navigation links. Filter by embedding status or search by name. **Export**: JSON font report with document filename, analysis date, full font inventory, issues, and compliance status. **Performance**: Lazy scanning with progress bar, quick scan mode (samples every 5th page for 200+ page docs), UI-responsive batching with setTimeout yields. Results cached. **Command palette**: Registered as "Inspect Fonts" and "Font Report". All processing client-side using pdf.js.

- [TASK-070] **NEW FEATURE** — Bookmark and outline editor with full CRUD operations. New module `js/bookmark-editor.js` extends the existing read-only bookmark panel with a complete editing interface. **Edit mode**: Toggle button switches between navigation and editing modes. In edit mode, each bookmark shows drag handle, rename (pencil icon or double-click), properties (gear icon), and delete (trash icon with confirmation) controls. **Create**: "Add" button creates a new bookmark at the current page and immediately opens inline rename. **Drag-and-drop**: Reorder bookmarks by dragging — drop above, below, or onto a bookmark to nest it as a child. Visual drop indicators (lines and highlight). Max nesting depth of 10 levels. **Properties dialog**: Modal for editing title, target page, Y position, zoom level (Fit Page, Fit Width, 50%-200%), bookmark color (RGB), bold/italic flags, and open/closed default state. **Auto-generate**: Scans all pages using pdf.js text content extraction, detects headings by font size (>1.3x median), creates bookmarks automatically. **Bulk operations**: Expand all, collapse all, delete all (with strong confirmation), flatten hierarchy. **Import/Export**: Export bookmark tree as JSON for external editing; import from JSON file. **Apply to PDF**: Uses pdf-lib to write the complete outline structure — creates `/Outlines` dictionary, builds linked-list of outline items with `/Title`, `/Parent`, `/Dest`, `/Prev`/`/Next`, `/First`/`/Last`/`/Count`, `/C` (color), and `/F` (flags). Handles FitH and XYZ destination types. Removes existing outlines before applying. Reloads document in viewer after save. **Save As**: Apply bookmarks and download as `[filename]-bookmarked.pdf`. All processing client-side using pdf-lib.

- [TASK-069] **NEW FEATURE** — Page resize and custom dimensions tool. New module `js/page-resize.js` adds a "Resize" button to the Pages toolbar that opens a dedicated resize panel. **Standard sizes**: Dropdown with A4, US Letter, Legal, A3, A5, B5, Tabloid, Executive, and Custom. Displays dimensions in the user's preferred unit (mm, inches, or points) with real-time conversion. Custom size inputs with min/max validation. **Orientation**: Portrait/landscape toggle that swaps width and height. **Content handling modes**: Four strategies — Scale to Fit (uniform scale, centered, preserving aspect ratio), Stretch to Fill (non-uniform, with distortion warning), Center No Scale (original size centered on new page), and Reposition (manual X/Y offset). **Page scope**: Apply to current page, all pages, custom range (e.g. "1-5, 8, 12-20"), or pages matching a specific detected size. Summary shows affected page count. Mixed-size document detection with independent scaling per page. **Margins**: Collapsible Advanced section with top/right/bottom/left margin inputs, link toggle for uniform margins, all in the user's selected unit. **Print production**: Collapsible Bleed section with configurable bleed amount and trim marks drawn at page corners. **Live preview**: Canvas preview showing the resized page with content placement, original page outline (dashed), new page border (solid), margin guides, and bleed area. Before/after toggle. Scale percentage display. 100ms debounce on parameter changes. **Implementation**: Uses pdf-lib's `embedPage()` and `drawPage()` to transfer all page content as a single XObject, preserving text, images, vector graphics, and annotations. Creates new pages with target dimensions, draws embedded original content with calculated placement, removes originals. Handles rotated pages, zero-dimension pages, and extreme sizes. Outputs `[filename]-resized.pdf`. All processing client-side.

- [TASK-068] **NEW FEATURE** — Text-to-speech PDF reader with synchronized word highlighting. New module `js/tts.js` implements a full TTS reading system using the Web Speech API. **Speech controls**: Floating control bar at the bottom of the viewer with play/pause, stop, skip forward/back by sentence, speed slider (0.5x–3x), volume slider, voice selector (grouped by language), and reading mode selector. **Text extraction**: Extracts text from PDF pages via pdf.js `getTextContent()`, sorts in reading order (top-to-bottom, left-to-right), handles ligatures (fi, fl, ff, ffi, ffl), soft hyphens, and multi-column layouts. Optional header/footer skipping. Caches extracted text per page. **Synchronized highlighting**: Canvas overlay on the PDF page highlights the current word or sentence in real-time using `SpeechSynthesisUtterance.onboundary` events. Configurable highlight color/opacity and word-level or sentence-level granularity. Auto-scrolls to keep highlighted text visible. **Reading modes**: Page mode (reads one page, then pauses), Continuous mode (reads through entire document with auto page advance), and Selection mode (reads user-selected text). **Chrome compatibility**: Breaks long text into ~200-char chunks at sentence/word boundaries to work around Chrome's utterance timeout bug, with 50ms gap between chunks. **Language detection**: Auto-detects document language from text content (supports EN, DE, FR, ES, ZH, JA, KO, RU, AR) and selects matching voice. **Tab visibility**: Auto-pauses when user switches tabs, resumes on return. **Progress tracking**: Page progress bar, sentence counter, and integration with reading progress tracker. **Preferences**: All settings (speed, volume, voice, highlight style, reading mode) persisted in localStorage. **Command palette**: Registered as "Read Aloud", "Pause Reading", "Stop Reading", "Increase/Decrease Reading Speed". Toolbar button with speaker icon added to viewer toolbar. All client-side — no server involvement.

- [TASK-066] **NEW FEATURE** — Undo/redo editing history with visual operation timeline. New module `js/undo-redo.js` implements a comprehensive Command pattern-based undo/redo system. **Command classes**: 15 concrete command types covering annotations (add/delete/move/edit), page operations (reorder/delete/rotate/insert/crop), text overlays, watermarks, form fields, drawings, stamps, signatures, and batch operations. `GenericCommand` for simple execute/undo pairs. `BatchCommand` groups multiple operations into a single undo entry. **UndoRedoManager**: Singleton manager with undo/redo stacks, configurable max history (100 ops), multi-step undo/redo-to-index, and event-driven state notifications via EventBus. `execute()` and `pushExecuted()` methods for integration with existing modules. **Toolbar buttons**: Undo/redo buttons added to main viewer toolbar with disabled state management and descriptive tooltips. Dropdown arrow next to undo shows last 10 operations for multi-step undo. **History sidebar panel**: New "History" tab in the sidebar (clock icon) with vertical timeline showing all operations chronologically. Each entry shows operation icon, description, relative timestamp, and clickable page badges for navigation. Current state divider separates undo (active) from redo (dimmed) entries. Click any entry to jump to that state. Counter shows operations used vs max. Clear button with confirmation. **Keyboard shortcuts**: Ctrl+Z for undo, Ctrl+Shift+Z/Ctrl+Y for redo. Skips interception when text inputs are focused. **Command palette**: Registered as "Undo", "Redo", "Show History Panel", "Clear History" actions. History auto-clears on new file load. All client-side.

- [TASK-065] **NEW FEATURE** — Digital signature verification and certificate inspection tool. New module `js/verify-signatures.js` adds a "Verify Signatures" sidebar panel (shield icon with checkmark). **Signature detection**: Scans raw PDF bytes for `/ByteRange` entries to locate all digital signature fields. Extracts signer name (`/Name`), date (`/M`), reason, location, contact info, filter, and sub-filter from the signature dictionary. **PKCS#7/CMS parsing**: Built-in ASN.1/DER parser decodes the PKCS#7 SignedData structure from the `/Contents` hex string. Extracts embedded X.509 certificates, digest algorithm, signature algorithm, signed attributes (signingTime, messageDigest), and unsigned attributes (timestamp tokens). **Integrity verification**: Computes hash (SHA-256/384/512/SHA-1) over the byte ranges covered by the signature using Web Crypto API, compares against the messageDigest in CMS signed attributes. Reports whether document has been modified since signing. Detects uncovered bytes after signature (potential shadow attacks). **Certificate inspection**: Modal displays full certificate details — subject, issuer, serial number, validity period (with expired/valid badge), public key algorithm and size, key usage extensions, certificate chain visualization as interactive tree. SHA-256 fingerprint computed via Web Crypto. **Timestamp detection**: Identifies RFC 3161 timestamp tokens in unsigned attributes, extracts TSA certificate name. **Status indicators**: Color-coded entries (green=valid, yellow=warning, red=invalid, gray=unknown) with shield icons. Expandable detail rows showing verification steps. Sidebar badge shows signature count. **Export report**: Generates downloadable TXT verification report with all signature details, certificate info, and integrity results. **Command palette**: Registered as "Verify Signatures" and "Check Document Integrity" actions. All processing client-side using Web Crypto API — no PDF data leaves the browser.

## 2026-04-04

- [TASK-064] **NEW FEATURE** — Multi-tab document workspace. New module `js/tabs.js` enables opening and editing multiple PDFs simultaneously in a tabbed interface. **Tab bar UI**: Horizontal tab bar below the toolbar with filename, PDF icon, modified indicator (dot), and close button per tab. Active tab highlighted with primary color accent border. Overflow handled with scroll arrows and mouse wheel horizontal scrolling. "+" button to open new files. **Tab management**: Open up to 10 simultaneous documents. Each tab maintains isolated state (currentFile, pdfDocument, page, zoom, tool). Switching tabs swaps the shared `state` object so all existing tools (50+ modules) work without modification. **Context menu**: Right-click tabs for Close, Close Others, Close to the Right, Close All, Duplicate, Copy Filename. **Keyboard shortcuts**: Ctrl+Tab/Ctrl+Shift+Tab to cycle tabs, Ctrl+W to close, Ctrl+T to open new, Ctrl+1-9 to jump by position. **Drag-and-drop**: Drop PDF files onto the tab bar to open in new tab. **Unsaved changes**: Modified indicator, confirmation dialog on close, `beforeunload` protection. **Theme support**: Light and dark mode compatible. All client-side.

- [TASK-063] **NEW FEATURE** — PDF to PowerPoint (PPTX) export. New module `js/pptx-export.js` adds a "To PPTX" sub-tab in the Convert panel. Uses PptxGenJS (v3.12.0, client-side, ~477KB bundle). **Export modes**: (1) "Image Only" — renders each PDF page to a high-res canvas and inserts as full-slide background image, best fidelity; (2) "Image + Text" — adds a hidden searchable text layer over the image using pdf.js text extraction, making slides searchable in PowerPoint; (3) "Text Only" — extracts text as native PowerPoint text boxes with position/font mapping, falls back to image for pages with insufficient text. **Slide sizing**: Slides match the PDF page aspect ratio (landscape/portrait auto-detected). Large pages (posters) clamped to PowerPoint's 56" max. **Quality control**: DPI selector (72/150/192/300) with size estimate. **Page range**: All pages, current page, or custom range (e.g., "1-5, 8, 12"). **Speaker notes**: Optional extraction of PDF annotations as slide speaker notes. **Performance**: Sequential page processing with UI yielding every 5 pages, progress bar, large document warning (50+ pages). **Text formatting**: Bold/italic detection from font names, font size mapping (PDF units to PowerPoint points), Arial fallback. **Output**: Downloads as `[filename].pptx`. All processing client-side.

- [TASK-061] **NEW FEATURE** — Embedded image manager for inspecting, replacing, and optimizing images within PDFs. New module `js/image-manager.js` adds an "Images" tab with image icon. **Image inventory**: Scans all pages using pdf.js `getOperatorList()` to catalog embedded image XObjects (JPEG and bitmap). Displays sortable/filterable gallery grid with thumbnails, dimensions, file size, format, and page references. Aggregates stats (total count, data size, pages with images). Deduplicates shared XObjects across pages. **Image preview**: Full-size preview modal with metadata. **Image replacement**: File picker for PNG/JPEG/WebP, embeds replacement via pdf-lib and swaps XObject references by matching dimensions. **Per-image optimization**: Dialog with DPI reduction slider, JPEG quality control, and grayscale conversion. Renders via canvas, re-encodes as JPEG, replaces in PDF structure. **Batch optimization**: Presets (Web 72DPI/75%, Print 300DPI/90%, Max 150DPI/60%), DPI and quality sliders, grayscale toggle. Processes all images with progress bar, shows before/after size comparison. **Duplicate detection**: Groups images by dimensions, flags potential duplicates. **Undo support**: Full undo stack (max 10) storing PDF ArrayBuffer snapshots. **Selection**: Click, Ctrl+click, Shift+click for multi-select. Sort by size/page/dimensions, filter by format. **Output**: Downloads as `*-optimized.pdf`. All processing client-side using pdf-lib and pdf.js.

- [SECURITY] **FIX** — Fixed 5 innerHTML injection points in `js/page-labels.js` (TASK-060). User-typed label prefix and prompt()-sourced custom preset names were inserted into innerHTML without HTML escaping. Added `escapeHtml()` function (escapes &, <, >, ", ') and wrapped all user-controlled values in `renderRangeList()`, `renderThumbnailStrip()`, `renderLabelMap()`, and `renderPresetList()`. Also fixed `escapeAttr()` to escape `&` (previously missing, allowing potential attribute breakout via `&quot;` sequences). All vectors were CSP-mitigated but violated defense-in-depth.

- [TASK-060] **NEW FEATURE** — Page label manager for custom page numbering schemes per section. New module `js/page-labels.js` adds a "Labels" button in the Pages tab for defining custom page numbering. **Label ranges**: Users define numbering ranges with configurable style (Arabic, Roman uppercase/lowercase, Alphabetic uppercase/lowercase, None), prefix string, starting number, and starting page. Multiple ranges supported for sectioned documents. **Visual editor**: Thumbnail strip showing all pages with color-coded label previews (blue=Arabic, purple=Roman, green=Alphabetic, gray=None). Range list with inline editing of style, prefix, start number, and start page. Add/remove ranges. **Presets**: Book (cover=none, preface=roman, body=arabic), Academic (title=none, front=roman, body=arabic, appendix=A-), Legal (all arabic), Simple/Reset. Custom presets saved to localStorage. **Label map**: Preview table showing page-to-label mapping for all pages. **Import/Export**: JSON configuration files for sharing across documents/devices. **Undo support**: Full undo stack for all label changes. **Existing label parsing**: Reads `/PageLabels` number tree from loaded PDFs via pdf-lib. **PDF output**: Writes `/PageLabels` to document catalog with proper `/S`, `/P`, `/St` entries per range. Saves as `*-labeled.pdf`. **Command palette**: Registered as "Page Labels" under Page category. All processing client-side using pdf-lib.

- [TASK-059] **NEW FEATURE** — Selective region extraction (snipping) tool. New module `js/snip.js` adds a "Snip" tab with scissors icon for drawing selection rectangles on PDF pages and extracting regions. **Selection UI**: Crosshair cursor, click-and-drag to draw blue selection rectangles with dimension labels (pt and mm). 8 resize handles for fine adjustment, drag to reposition. Shift constrains to square. Multiple selections per page with numbered badges. **Extract as image**: Render selected region via pdf.js at configurable resolution (1x–4x), export as PNG or JPG (quality slider 60–100%), or copy to clipboard via ClipboardItem API. **Extract as PDF**: Uses pdf-lib `embedPages()` with clip boundaries to preserve vector content and text selectability. **Extract text**: Identifies text items intersecting selection via `getTextContent()`, copies to clipboard in reading order. **Mini toolbar**: Appears below selection with one-click buttons for Copy Image, PNG, JPG, PDF, Copy Text, Delete. **History panel**: Last 10 snips with page/format/timestamp, clickable to navigate. **Keyboard shortcuts**: Escape=deselect, Delete=remove, Enter=save PNG, arrow keys=nudge, Ctrl+A=select page. **Command palette**: Registered as "Snip Region", "Extract Region", "Selection Tool". Settings panel with resolution selector and JPG quality control.

- [TASK-057] **NEW FEATURE** — PDF document sanitization tool ("Clean for Sharing"). New module `js/sanitize.js` adds a "Sanitize" tab with shield icon for stripping hidden data before distribution. **Scan engine**: Inspects PDF structure via pdf-lib low-level API to detect 10 categories of hidden data: document metadata (Info dict fields + custom properties), JavaScript (Names tree, OpenAction, catalog/page AA actions), embedded file attachments (EmbeddedFiles name tree), comments & annotations (all non-Link annotation subtypes per page), filled form field data (text, checkbox, dropdown values), hidden OCG layers (OFF array in OCProperties default config), page thumbnails (Thumb entries), document history (incremental saves — cleaned automatically by pdf-lib save), sensitive bookmarks (titles matching email/internal-URL/confidential patterns), and internal URL links (localhost, RFC1918, .internal/.local/.corp domains). **Risk indicator**: Color-coded dot (green/yellow/red) with summary of hidden data types found. **Checklist UI**: Each category shows item count, expand arrow for details (metadata field values, JS snippets, attachment filenames, annotation counts per page, form field values, layer names, bookmark titles, internal URLs). Individual checkboxes for selective removal. **Presets**: Maximum (all), Privacy (metadata+comments+forms+thumbnails+history), Security (JS+attachments+layers+internal links), Legal Discovery (keep annotations, strip rest), Custom. **Sanitization**: Creates clean copy via pdf-lib — clears Info dict (with optional author replacement), removes JS name trees and actions, strips EmbeddedFiles, filters non-Link annotations, clears form values, removes OCG OFF entries, deletes Thumb entries, renames sensitive bookmarks to "(removed)", removes internal-URL link annotations. Outputs `*-sanitized.pdf`. **Report**: Downloadable text report listing all removed items with before/after file sizes. Progress bar throughout scan and sanitize operations.

- [TASK-056] **NEW FEATURE** — PDF hyperlink extraction, validation, and batch editing. New module `js/link-manager.js` adds a "Links" tab for comprehensive hyperlink management. **Link extraction**: Scans all pages via pdf.js `getAnnotations()`, categorizes links as URI, Internal (GoTo), Remote (GoToR), or Named Action, and extracts display text by cross-referencing annotation rects with `getTextContent()`. **Link list panel**: Scrollable, sortable table with columns for page, display text, URL, type (color-coded badges), and validation status. Search/filter by text, type, status, and page range. Summary bar with link counts. Click to navigate and highlight link on page. **Link validation**: Batch validation of external URLs via `fetch()` HEAD requests with no-cors fallback, 10s timeout, concurrency limit of 5. Classifies responses: valid (2xx), redirect (3xx), broken (4xx), error (5xx). Internal links verified against page count. Progress bar during validation. Results cached per session. **Batch URL editing**: Find & Replace dialog with regex support, preview of all affected URLs before applying. Uses pdf-lib to modify `/URI` annotation values in the PDF. Individual inline URL editing. **Link removal**: Multi-select checkboxes, removes annotation objects from PDF via pdf-lib while preserving text. **Export reports**: CSV (UTF-8 BOM), JSON (with metadata), standalone HTML report with styled table and color-coded statuses. **Visual overlay**: Toggle color-coded link highlights on pages (blue=valid, red=broken, yellow=redirect, green=internal, gray=unchecked) with hover tooltips. CSS styles in tools.css, nav tab with chain-link icon.

- [SECURITY] Reviewed TASK-054 (html-export.js, 688 lines) and TASK-055 (form-data.js, 908 lines + forms.js update). Both modules secure — all user/PDF content escaped, no eval/Function/document.write, no external requests, sanitized filenames. form-data.js batch fill enforces 500-record limit. Added form-data.js to escHtml double-quote attribute context tracking (LOW, CSP mitigated).

- [TASK-054] **NEW FEATURE** — Export PDF to HTML with responsive layout. New module `js/html-export.js` adds a "To HTML" sub-tab in the Convert panel. **Two layout modes**: Reflowable (semantic HTML with `<h1>`-`<h3>`, `<p>`, `<ul>`/`<ol>`, bold/italic detection — responsive and accessible) and Fixed (absolute-positioned text items preserving exact PDF layout at 800px width). **Three output formats**: single self-contained HTML file, multi-file ZIP (separate CSS via JSZip), or HTML snippet (body-only for CMS embedding). **Text extraction pipeline**: pdf.js `getTextContent()` → line grouping by Y-tolerance → paragraph grouping by gap analysis → heading detection by font size ratios → list detection by bullet/number patterns → inline formatting from font names. Image-only pages fall back to canvas-rendered PNG data URIs at configurable DPI (72/150/300). **Three themes**: Clean (sans-serif, white), Document (serif, cream/paper), Dark (dark background). Configurable base font size (12-24px slider). Navigation sidebar with heading anchors and page links (responsive — collapses on mobile). Back-to-top button. Print stylesheet hides nav. Page range selection (all/current/custom). Batch processing with progress bar and UI-responsive setTimeout yielding.

- [TASK-055] **NEW FEATURE** — Form data import/export and batch fill. New module `js/form-data.js` extends the Forms tab with comprehensive data workflows. **Export form data** in 5 formats: JSON (with metadata wrapper), CSV (UTF-8 BOM for Excel), XML (structured with field types), FDF (Adobe Forms Data Format with proper escaping), and XFDF (XML-based FDF). Handles duplicate field names by appending suffixes. **Import form data** from JSON/CSV/XML/FDF/XFDF with mapping preview table showing auto-matched fields (exact, case-insensitive, fuzzy matching of underscores/hyphens/spaces), unmatched fields in red with manual mapping dropdowns, per-field include/exclude checkboxes. Data validation: boolean parsing for checkboxes, option validation for dropdowns, maxLength truncation for text fields. **Batch fill** — fill the same PDF form template with multiple records from CSV or JSON array. Visual field mapping editor with auto-mapping and saved mapping configurations (localStorage). Configurable filename templates ({original}, {n}, {fieldValue:ColumnName}). Download options: ZIP archive (JSZip), merged single PDF, or individual files. Progress bar with per-record updates, UI-responsive processing (setTimeout batching), 500-record cap. **Template management** — save/delete form field structures to localStorage for reuse without re-uploading the PDF. **Form fields summary** — copyable table of all field names, types, pages, values, and options. Added exports `getFormFields()` and `setFormFieldValues()` to forms.js. UI integrated as collapsible sub-panels in the Forms tab. CSS styles in tools.css.

- [TASK-053] **NEW FEATURE** — PDF accessibility checker and compliance report generator. New module `js/accessibility.js` analyzes PDFs against WCAG 2.1 and PDF/UA guidelines. **8 automated checks** across 6 categories: (1) Document Structure — tagged PDF detection via /MarkInfo, (2) Text Alternatives — image detection via operator list (paintImageXObject), flags unverifiable alt text in untagged docs, (3) Color & Contrast — text color extraction via setFillRGBColor operators with WCAG luminance-based contrast ratio calculation against white background, (4) Navigation — bookmark/outline detection, heading hierarchy analysis via font size comparison to dominant body size, (5) Forms — AcroForm field label (/TU tooltip) verification, (6) Metadata — /Lang entry, /Title, /DisplayDocTitle checks. **Dashboard** with color-coded score badge (green/yellow/red), pass/fail/warning counts. **Categorized results** with expandable sections, severity badges (critical/major/minor), WCAG criterion references (e.g. 1.1.1 Non-text Content, 1.4.3 Contrast Minimum). **Quick fixes** for missing language and DisplayDocTitle via pdf-lib catalog manipulation, saves as "-accessible.pdf". **Export** as structured PDF report (pdf-lib generated with word-wrapped descriptions) or machine-readable JSON. Page sampling for large documents (50 pages for images, 30 for headings, 10 for contrast). Clickable page references navigate to affected pages. Progress bar during analysis. Nav tab ("A11y" with accessibility icon), tool panel HTML, CSS styles in tools.css added.

## 2026-04-03

- [TASK-034] **BUG FIX** — Hyperlink internal page navigation was broken. The `navigate:page` event emitted by `hyperlinks.js` (and `autosave.js`) had no listener. Added `bus.on('navigate:page', (pageNum) => goToPage(pageNum))` in `viewer.js` init function to wire up page navigation for internal PDF links, link list panel clicks, and autosave session restore.
- [TASK-050] **NEW FEATURE** — Headers, footers, and page numbering tool. New module `js/headers-footers.js` adds a comprehensive header/footer editor. **6 configurable zones** (top-left, top-center, top-right, bottom-left, bottom-center, bottom-right) with visual grid layout matching page positions. **Dynamic tokens**: `{page}`, `{pages}`, `{date}`, `{filename}`, `{title}` with one-click insertion buttons per zone. **5 numbering styles**: Arabic, Roman lowercase/uppercase, alphabetic lowercase/uppercase. **Page controls**: start number offset, skip first page toggle, custom page range (e.g. "2-10, 15"), odd/even page alternation (book-style mirroring of left/right zones). **Typography**: Helvetica/Times/Courier with bold/italic variants (all 12 pdf-lib StandardFonts), font size 6-24pt, color picker, opacity slider. **Margins**: configurable top/bottom/horiz margins with visual margin guides in preview. **Separator lines**: optional horizontal rules between header/footer and page content. **Date formats**: YYYY-MM-DD, MM/DD/YYYY, DD/MM/YYYY, MMM DD YYYY. **5 built-in presets**: "Page numbers bottom center", "Title & date header", "Legal footer", "Book style", "Academic paper". Custom presets save to localStorage. **Live preview** with debounced canvas rendering, page navigator for verifying different pages. **Remove existing headers/footers** mode using pdf.js text extraction + pdf-lib white-rectangle redaction. Text truncation with ellipsis for overflow. All processing client-side via pdf-lib. Nav tab, tool panel HTML, CSS styles added.
- [SECURITY] **XSS FIX** — `templates.js` line 688: custom template names from JSON imports were used unescaped in `aria-label` HTML attribute, allowing attribute injection via crafted import files. Fixed by wrapping with `escHtml()`. Also hardened import validation: `templateId` now validated against known generators whitelist, name truncated to 100 chars, strict type checks on all imported fields. CSP mitigated script execution risk but DOM injection was possible.
- [TASK-049] **NEW FEATURE** — Pre-built document templates system. New module `js/templates.js` generates PDFs entirely client-side using pdf-lib. 12 built-in templates across 4 categories: **Basic** (Blank Page, Lined Paper, Graph Paper, Dot Grid), **Education** (Cornell Notes, Sheet Music), **Business** (Invoice, Letterhead, Certificate, Meeting Agenda), **Productivity** (To-Do List, Weekly Planner). Each template opens a configurator with common options (page size A4/Letter/Legal/A3, portrait/landscape, page count 1-500, margin, color theme classic/blue/sepia/dark) plus template-specific options (line spacing, grid size, stave count, certificate type, checkbox rows, planner hours, etc.). Live canvas preview at 50% scale updates as settings change. Batched multi-page generation with progress bar for >10 pages. Custom templates saved to localStorage with rename/delete and JSON import/export. Welcome screen shows 4 popular template quick-start cards. Generated PDFs load directly into the viewer for immediate editing. Templates nav tab, tool panel HTML, and CSS added.
- [TASK-048] **NEW FEATURE** — Command palette and customizable keyboard shortcuts. Three new modules: `js/action-registry.js` (central registry for all app actions with unique IDs, categories, descriptions, default shortcuts, and execute callbacks), `js/command-palette.js` (Ctrl+K opens a modal overlay with fuzzy search across all registered actions — shows recent actions, category headers, match highlighting, keyboard navigation with arrow keys/Enter/Escape), `js/keyboard-shortcuts.js` (global keydown handler dispatches registered shortcuts, customization panel with per-action edit/capture mode, conflict detection, export/import JSON, reset to defaults, printable cheat sheet). ~50 built-in actions registered across File, View, Page, Annotate, Tools, Convert, and Settings categories. Custom bindings persist to localStorage. Command palette button added to header. Browser-reserved shortcuts (Ctrl+T/W/N etc.) blocked from capture. Full ARIA accessibility (dialog, listbox, activedescendant).
- [TASK-047] **ENHANCED FEATURE** — Page crop and trim tool. Extended the existing crop module with: aspect ratio constraints (A4, Letter, Square, 16:9, custom W:H), extended apply modes (all pages, current page, even/odd pages, custom page range), "Also resize MediaBox" checkbox for irreversible size reduction, undo integration via pages module's `pages:save-undo` event (Ctrl+Z reverts crops), rotated page handling (adjusts crop coordinates for 90°/270° rotated pages), minimum 50×50pt crop area enforcement, and "No whitespace margins detected" message for auto-crop. Updated: `js/crop.js`, `index.html` (crop panel HTML), `css/tools.css` (new control styles).
- [TASK-024] **BUG FIX** — Added undo support for page insertion. `insert-pages.js` now emits `pages:save-undo` before applying insertions. `pages.js` saves pre-insertion state (page items + PDF bytes) onto the undo history stack. Ctrl+Z fully reverts insertions by restoring both page metadata and the original PDF document via pdf.js reload. Also added `_skipPdfReady` flag to prevent `onPdfReady` from rebuilding state during undo restore.
- [SECURITY] **XSS FIX** — `qrbarcode.js` line 682: user-supplied QR/barcode data (`p.codeData`) was interpolated into `innerHTML` via template literal without HTML escaping in the placement list UI. Added `escapeHtml()` function and applied it to sanitize the output. CSP `script-src 'self'` mitigated inline script execution, but the raw HTML injection was still a risk for UI manipulation.
- [TASK-041] **NEW FEATURE** — Measure & distance tool for PDF pages. Three measurement modes: Distance (click two points for straight-line measurement with angle display), Perimeter (click multiple points to form a polyline, double-click or Enter to finish), and Area (click to define a closed polygon with semi-transparent fill). Scale calibration system: click two known-distance points, enter real-world measurement with unit selection (points, inches, cm, mm, feet, meters) — stored per-page. SVG overlays rendered in normalized 0–1 coordinates for zoom independence, with colored lines/polygons (blue/green/orange) and circular handles. Point snapping to page edges, corners, and other measurement points within 5px tolerance. Crosshair cursor guides while placing points. Measurement list panel grouped by page with click-to-navigate and per-item delete. Undo history (30 deep), keyboard shortcuts (Escape cancel, Delete remove, Ctrl+Z undo, Enter finish). PDF annotation persistence via pdf-lib — embeds measurement lines, polygons, circles, and value labels into the PDF, output as `-measured.pdf`. New file: `js/measure.js`. Nav tab, panel HTML, and CSS added.

## 2026-04-02

- [TASK-039] **NEW FEATURE** — Interactive PDF form creation tool: enables users to author form fields on any PDF page. Adds a "Create Form" mode within the Forms tab with five field types (text input, checkbox, radio button, dropdown select, push button). Users click-and-drag on the PDF canvas to draw field bounding rectangles with live blue dashed preview. Placed fields rendered as semi-transparent colored overlays (blue/green/orange/purple/gray by type) with field name labels. Selection with 8-point resize handles, multi-select via Ctrl+click and box-select. Properties panel for editing field name, position, size, font size, options, required/read-only flags. Snapping to page edges, other field edges, and centers with red guide lines. Alignment toolbar (left/right/top/bottom/center/distribute). Keyboard shortcuts: Delete, Ctrl+D duplicate, Ctrl+Z/Y undo/redo (50-entry history), arrow nudge (1pt/10pt with Shift), Tab to cycle, Ctrl+A select all. Imports existing form fields from loaded PDFs. Generates form-enabled PDF via pdf-lib with proper AcroField types (PDFTextField, PDFCheckBox, PDFRadioGroup, PDFDropdown, PDFButton) and NeedAppearances flag. Outputs as `[filename]-form.pdf`. New file: `js/form-creator.js`. CSS added to `tools.css`.
- [TASK-013] **BUG FIX** — Watermark "Below content" layer option now works correctly. Replaced non-functional `drawTextBelow()`/`drawImageBelow()` stubs (which just called the same above-content draw methods) with `reorderContentBelow()`, which uses pdf-lib's `PDFName` API to reorder the page's `Contents` array so the watermark content stream is placed before existing page content streams. PDF renders streams in array order, so the watermark now correctly appears behind existing content when "Below content (background)" is selected.
- [TASK-037] **NEW FEATURE** — Table extraction tool: detect tabular data in PDF pages and export as CSV, XLSX, or copy to clipboard. Grid-based detection clusters text items by Y-coordinate into rows, identifies column boundaries via X-position analysis, and builds 2D cell grids. Supports scanning current page, all pages, or custom ranges with progress bar. Detected tables shown as clickable result cards with data preview (first 3 rows). Full editable HTML table preview for correcting cells before export. Manual region selection fallback — draw rectangle on PDF page with optional column count override. CSV export with UTF-8 BOM for Excel compatibility. XLSX export via SheetJS with auto-column-width and number detection. Tab-separated clipboard copy. Options: header row detection, cell trimming, row tolerance slider. Added as "Tables" sub-tab under Convert panel. SheetJS (xlsx.full.min.js) added to lib/.
- [TASK-034] **NEW FEATURE** — Hyperlink annotations tool: add, edit, detect, and navigate links in PDFs. Detects existing `/Link` annotations from loaded PDFs via pdf.js and renders them as interactive overlays (blue for URL, green for internal page, orange for email). Users can click-and-drag to create link regions with a dialog for URL/page/email input. Double-click to edit, right-click context menu for edit/copy/delete. "Show All" toggle highlights all links with colored overlays. Link list sidebar panel organizes all links by page with click-to-navigate. Undo/redo support (Ctrl+Z/Ctrl+Shift+Z). PDF export embeds links as proper `/Link` annotations with `/URI` and `/GoTo` action dictionaries via pdf-lib. Output as `-linked.pdf` when only hyperlinks are modified.
- [TASK-033] **NEW FEATURE** — Continuous scroll & dual-page spread view modes for the PDF viewer. Added a 3-button view mode selector to the toolbar (single page / continuous scroll / dual-page spread). Continuous scroll mode renders all pages vertically with IntersectionObserver-based lazy loading (renders pages near viewport, unloads distant ones to conserve memory), updates page indicator on scroll via requestAnimationFrame. Dual-page spread mode displays side-by-side page pairs with a cover-page toggle (page 1 alone vs paired). Both modes integrate with existing zoom, navigation, thumbnails, keyboard shortcuts, and all overlay modules (annotations, search, etc.). View mode and cover preference persisted in localStorage. Emits `viewmode:change` event on the bus for future module adaptation.
- [TASK-031] **NEW FEATURE** — Extract Images tool: added a third sub-tab under Convert that scans PDF pages for embedded images using pdf.js operator lists (paintImageXObject / paintJpegImageXObject). Displays found images in a gallery grid with thumbnails, dimensions, page number, and file size. Supports click/Shift+click/Ctrl+A selection, PNG/JPG export format with quality slider, single-image direct download, multi-image ZIP bundling with page-based subfolder structure via JSZip, clipboard copy, progress bars, large image warnings (>10MB), and empty state for imageless PDFs. JPEG images are extracted as raw bytes to avoid quality loss.

## 2026-04-01

- [TASK-016] **SECURITY FIX** — Redaction tool: rewrote `applyRedactions()` to use page-flattening instead of merely drawing opaque rectangles over content. Redacted pages are now rendered to a canvas via pdf.js, redaction rectangles are drawn on top (physically destroying pixel data), and the flattened raster image replaces the original page in the output PDF via pdf-lib. This guarantees no underlying text, vector, or image data survives. Unredacted pages are copied as-is to preserve quality. Updated confirm dialog to accurately describe the flattening process. Removed unused `rgb` import.
- [TASK-020] Fixed crop tool bugs: (1) CRITICAL — changed `[...mb]`/`[...cb]` array spread to `{ ...mb }`/`{ ...cb }` object spread on line 684, fixing `TypeError: mb is not iterable` crash in applyCropAndDownload(). (2) Implemented proper undo that rebuilds the PDF with original mediaBox/cropBox values and downloads the uncropped version, replacing the previous no-op that only reset the UI overlay.
- [TASK-021] Page numbers & headers/footers: implemented page-numbers.js with full header/footer tool under the Pages tab. Supports page number formats ("1", "Page 1", "Page 1 of N", Roman numerals), custom text with placeholders ({page}, {total}, {date}, {filename}), 6-position grid (top/bottom x left/center/right), font family (Helvetica/TimesRoman/Courier), font size (8-24pt), color picker, configurable margin, page range selection (all/skip first/custom), and live canvas preview. Uses pdf-lib drawText() for embedding. Output as -numbered.pdf. All 21 DOM IDs match, CSS styles defined, responsive layout. HTTP 200 from live site.
- [TASK-010] Digital signatures: implemented signatures.js (~550 lines) with three creation modes — draw on canvas (with color/thickness controls, touch support, auto-trim whitespace), type with 4 font choices (cursive/serif/sans/mono with live preview), and upload image (PNG/JPG/WebP with drag-and-drop). Signatures placed on PDF pages with drag-to-reposition and corner-resize handles. PDF embedding via pdf-lib embedPng/embedJpg with correct coordinate transformation. Integrated into main download flow in annotate.js. Output as -signed.pdf. Full keyboard accessibility (Delete to remove, focus indicators). Responsive CSS. All files serve HTTP 200.

## 2026-03-31

- [SECURITY] Reviewed new watermark.js (TASK-013) and redact.js (TASK-016) modules. Both secure: no unsafe innerHTML, no eval/Function/document.write. 33 checks passed, 0 critical findings. Flagged redaction limitation: opaque rectangles cover content visually but pdf-lib cannot strip underlying content streams — hidden data may still be extractable.
- [TASK-002] PDF viewer component: enhanced viewer.js with text layer rendering for text selection, fit-page zoom option, keyboard shortcuts (arrows for page nav, Ctrl+/-/0 for zoom, Home/End for first/last page), loading indicator during PDF load. Fixed tab switching to properly show/hide viewer vs tool panels.
- [SECURITY] Added 5 missing security headers to Nginx config (X-Frame-Options, X-Content-Type-Options, X-XSS-Protection, Referrer-Policy, Content-Security-Policy). Were documented in security-guide.md but never implemented. Also added client_max_body_size 50M to main server block.
- [TASK-003] File upload/download system: created js/upload.js module with download button handler, full-page drag-and-drop overlay, upload progress bar, and Ctrl+S/Cmd+S save shortcut. Added CSS for drop overlay and progress indicator.
- [TASK-001] Project scaffolding complete: directory structure, HTML shell with navigation, 3 CSS files, 11 JS ES modules, pdf.js/pdf-lib/Tesseract.js libraries downloaded to lib/
- [PIVOT] Complete system pivot from CronLoop dashboard to PDF Editor web application
- [PIVOT] Rewrote all 7 agent prompts for PDF editor development
- [PIVOT] Reset task board with 10 initial PDF editor tasks (TASK-001 to TASK-010)
- [PIVOT] Cleaned web root, created "Coming Soon" placeholder page
- [PIVOT] Rewrote CLAUDE.md, README.md, and all documentation for PDF editor context
- [PIVOT] Removed ~90 dashboard-specific scripts, kept 6 core orchestration scripts
- [PIVOT] Simplified cron-orchestrator.sh (removed dashboard status update hooks)
- [PIVOT] Updated cron schedule: 2-hour pipeline, 2-hour supervisor, hourly maintenance, daily cleanup
- [PIVOT] Git tagged pre-pivot state as v1.0-cronloop-dashboard
